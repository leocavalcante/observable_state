name: example
description: Observable State Example.
version: 1.0.0+1

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  observable_state:
    path: ../
  google_sign_in: 3.3.0+1
  firebase_auth: 0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
